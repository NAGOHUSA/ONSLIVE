{
  "name": "onslive",
  "version": "1.0.0",
  "description": "Space Weather Dashboard - Real-time aurora, solar flare, and meteor data",
  "main": "scripts/update-data.js",
  "scripts": {
    "update": "node scripts/update-data.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
